extends layouts/main
include mixins/videoBlock
//-
    main의 레이아웃을 확장하고 추가적인 것을 더하기 위해 1행 내용 삽입
    메인의 내용을 복붙함
    ==>copy whole layouts/main.pug so we can overwrit
    //-item(video) 대신 다른 문자 사용 가능하나 videos는 컨트롤러에서 온 것과 같아야함
block content
   .home-videos
        each item in videos
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            